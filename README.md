# algorithmic-stable-vault
A collateralized debt position (CDP) engine for minting a synthetic stablecoin. Users deposit Ether as collateral to mint 'STBL' tokens, maintaining a minimum collateralization ratio with built-in liquidation logic.
